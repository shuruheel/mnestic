[package]
name = "cozo-core-examples"
version = "0.1.0"
edition = "2021"

[dependencies]
cozo = { package = "mnestic", version = "0.10.5", path = "../cozo-core", default-features = false, features = [
    "rayon",
] }
